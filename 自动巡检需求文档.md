# 服务器自动巡检功能需求说明

---

## 1. 功能背景

通过 Prometheus 监控系统采集服务器资源指标，结合阿里云百炼智能体生成专业的巡检分析报告，替代人工巡检流程。

---

## 2. 功能需求

### 2.1 巡检触发

| 触发方式 | 说明 |
|---------|------|
| 手动触发 | 用户在界面上点击按钮，立即发起巡检 |
| 定时触发 | 系统按 cron 表达式自动执行（默认：每月 1 号 9:00） |

### 2.2 指标采集

通过 Prometheus HTTP API 查询以下指标：

| 指标 | 查询语句 | 单位 |
|------|---------|------|
| CPU 使用率 | `100 - (avg by (instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)` | % |
| 内存总量 | `node_memory_MemTotal_bytes` | bytes |
| 内存可用 | `node_memory_MemAvailable_bytes` | bytes |
| 磁盘总量 | `node_filesystem_size_bytes{fstype!~"tmpfs\|fuse.*", mountpoint!~"/boot.*"}` | bytes |
| 磁盘可用 | `node_filesystem_avail_bytes{fstype!~"tmpfs\|fuse.*", mountpoint!~"/boot.*"}` | bytes |

**衍生计算**：
- 内存使用率 = (总量 - 可用) / 总量 × 100
- 磁盘使用率 = (总量 - 可用) / 总量 × 100
- 实例 IP 从 Prometheus 的 `instance` 标签中提取，去掉端口部分

### 2.3 AI 报告生成

将采集到的指标格式化为结构化文本，发送给百炼智能体应用，返回 Markdown 格式的巡检报告。

**发送给 AI 的 Prompt 格式**（字节转 GB，保留 2 位小数）：

```markdown
# 服务器资源数据摘要

## 实例：192.168.1.10

### CPU 使用情况
- CPU 使用率：18.32%

### 内存使用情况
- 总内存：32.00 GB
- 已用内存：14.67 GB
- 可用内存：17.33 GB
- 内存使用率：45.84%

### 硬盘分区使用情况
#### 挂载点：/
- 设备：/dev/vda1
- 总容量：50.00 GB
- 已用：12.88 GB
- 可用：37.12 GB
- 使用率：25.76%
```

**调用参数**：

| 参数 | 来源 |
|------|------|
| api_key | 系统字典配置 `BAILIAN_API_KEY` |
| app_id | 系统字典配置 `BAILIAN_APP_ID` |
| prompt | 上面的结构化文本 |

**重试策略**：最多 3 次，指数退避（2s → 4s → 8s）

### 2.4 报告管理

| 操作 | 说明 |
|------|------|
| 列表查询 | 分页展示，支持按状态过滤、排序 |
| 查看详情 | 查看报告元信息 + 实例指标 + AI 生成的 Markdown 内容 |
| 下载 | 导出为 `.md` 文件，支持中文文件名 |
| 删除 | 批量删除，关联的实例指标级联删除 |
| 重试 | 对失败报告重新触发生成 |

### 2.5 过期清理

- 每次成功生成报告后，自动删除超过保留月数（默认 12 个月）的历史报告
- 删除报告时级联删除关联的实例指标

---

## 3. 数据模型

### 3.1 巡检报告表 `prometheus_inspection_report`

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | BIGINT PK | 是 | 主键 |
| `report_name` | VARCHAR(255) | 是 | 报告名称，格式：`{年}年{月}月服务器巡检报告_{YYYYMMDD_HHMM}` |
| `report_date` | DATE | 是 | 报告日期 |
| `status` | VARCHAR(20) | 是 | `pending` / `success` / `failed`，默认 `pending` |
| `instance_count` | INT | 否 | 本次巡检的服务器数量 |
| `generation_duration` | INT | 否 | 生成耗时（秒） |
| `markdown_content` | LONGTEXT | 否 | AI 生成的 Markdown 报告 |
| `prometheus_endpoint` | VARCHAR(500) | 否 | 本次使用的 Prometheus 地址 |
| `trigger_type` | VARCHAR(20) | 是 | `scheduled`（定时）/ `manual`（手动） |
| `error_message` | TEXT | 否 | 失败时的错误信息 |
| `created_by` | BIGINT | 否 | 创建人 ID |
| `created_time` | DATETIME | 是 | 创建时间 |
| `updated_time` | DATETIME | 是 | 更新时间 |

**索引**：`report_date`, `status`, `created_time`

### 3.2 实例指标表 `prometheus_inspection_instance_metric`

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | BIGINT PK | 是 | 主键 |
| `report_id` | BIGINT FK | 是 | 关联报告 ID，ON DELETE CASCADE |
| `instance_ip` | VARCHAR(100) | 是 | 服务器 IP |
| `instance_name` | VARCHAR(255) | 否 | 实例名称 |
| `cpu_usage_percent` | DECIMAL(5,2) | 否 | CPU 使用率（%） |
| `memory_total_bytes` | BIGINT | 否 | 内存总量（字节） |
| `memory_used_bytes` | BIGINT | 否 | 已用内存（字节） |
| `memory_available_bytes` | BIGINT | 否 | 可用内存（字节） |
| `memory_usage_percent` | DECIMAL(5,2) | 否 | 内存使用率（%） |
| `disk_partitions` | JSON | 否 | 磁盘分区列表 |
| `collected_time` | DATETIME | 是 | 指标采集时间 |

**索引**：`report_id`, `instance_ip`

**disk_partitions 结构**：
```json
[
  {
    "mount_point": "/",
    "device": "/dev/sda1",
    "total_bytes": 1000000000,
    "used_bytes": 500000000,
    "available_bytes": 500000000,
    "usage_percent": 50.0
  }
]
```

### 3.3 系统配置项（字典表）

字典类型：`prometheus_inspection`

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| `PROMETHEUS_ENDPOINT` | `http://192.168.2.161:9090` | Prometheus API 地址 |
| `LABEL_FILTERS` | （空） | 实例标签过滤，可选 |
| `RETENTION_MONTHS` | `12` | 报告保留月数 |
| `BAILIAN_API_KEY` | （需配置） | 百炼 API Key |
| `BAILIAN_APP_ID` | `e579a36217c64c219ca64d1099c98299` | 百炼智能体应用 ID |

---

## 4. 接口设计

**前缀**：`/inspection/report`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/list` | 分页查询报告列表，支持状态过滤 |
| GET | `/detail/{id}` | 报告详情（含实例指标） |
| POST | `/generate` | 手动触发巡检，异步执行，立即返回 |
| GET | `/download/{id}` | 下载报告 .md 文件（RFC 5987 中文文件名） |
| DELETE | `/delete` | 批量删除，请求体 `{ "ids": [1,2,3] }` |
| GET | `/instances/{id}` | 获取指定报告的实例指标列表 |
| POST | `/retry` | 重试失败报告，请求体 `{ "report_id": 1 }` |

---

## 5. 状态流转

```
        触发生成
           ↓
        [pending]
       ↙        ↘
  生成成功       生成失败
     ↓             ↓
 [success]     [failed]
                   ↓
               点击重试 → [pending]
```

- **pending**：正在生成中（前端显示：待处理，灰色）
- **success**：生成完成（前端显示：已完成，绿色）
- **failed**：生成失败，保留 error_message（前端显示：失败，红色）

**关键约束**：状态变更后必须立即持久化，不能等整体业务事务结束。

---

## 6. 权限点

| 权限编码 | 说明 |
|---------|------|
| `inspection:report:query` | 查看报告列表和详情 |
| `inspection:report:create` | 手动触发巡检、重试 |
| `inspection:report:download` | 下载报告 |
| `inspection:report:delete` | 删除报告 |

---

## 7. 非功能性要求

1. **异步执行**：报告生成是耗时操作（含外部 API 调用），接口必须异步返回
2. **状态可靠**：pending/success/failed 三种状态必须都能可靠保存
3. **失败可追溯**：失败报告保留 error_message，支持查看原因和重试
4. **中文支持**：下载文件名需正确编码（RFC 5987）
5. **数据清理**：自动清理过期数据，防止无限增长
